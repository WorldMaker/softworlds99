// Edit this file for local viewing of the pages.

sitname = "SoftWorlds"		// Your site name, appears on the top
				// of the bar.

sitid = "sw"			// Your site id.  Used by sitbar.js
				// for multiple pages using the same sitbar.  
				// (My sitbar also has sites "ygg" and
				// "az")

sitsubid = "top"		// The bar uses the Site Sub ID to determine
				// which sections to display in the bar.


baseadd = "file://c:/download/softworlds"
				// Base address used by all links.
				// Don't add the last slash because
				// addresses in the sitbar.js look like
				// "/bob.htm"

homdress = "file://c:/download/wmonline/index.html"
				// Location of the top-most page.
				// I use this as a link to my personal
				// page.

barlog = "file://c:/download/softworlds/images/wm.gif"
				// Image file for the Home Address link.
				// I use size: 150x35

sitlog = "file://c:/download/softworlds/images/sw.gif"
				// Image file for the Site Logo
				// I use size: 150x35 for Site Logos.